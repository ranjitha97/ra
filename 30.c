#include<stdio.h>
int main()
{
    int i, n=15, sum=0;
    
    printf("Enter upper limit: ");
    scanf("%d", &n);
void num()
    {
    for(i=1; i<=n; i++)
    {
        sum += i;
    }

    printf("Sum of first %d natural numbers = %d", n, sum);
}
void odd()
int a=45;i;sum=0;
{
for(i=15;i<=a;i++)
if(n%2!=0)
{
sum+=i;
}
printf("sum of odd number=%d"sum);
}
raturn 0
}
